<Window x:Class="ASofCP.Cashier.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:AttachedProperties="clr-namespace:ASofCP.Cashier.AttachedProperties"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        Title="{Binding Title}" 
        Height="325" Width="525"
        MinHeight="325" MinWidth="525"
        MaxHeight="325" MaxWidth="525"
        WindowStartupLocation="CenterScreen">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadedCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>


    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>


        <TextBlock Text="Представьтесь пожалуйста" Margin="0,15,0,2"
                   HorizontalAlignment="Left"
                   Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="3" />
        
        <StackPanel Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Top"
                    Margin="0,25,0,10" Grid.Column="2" Grid.Row="1">
            <TextBlock Text="Кассир" Margin="0,0,0,2"
                       FontWeight="SemiBold" />
            <ComboBox ItemsSource="{Binding Users}" SelectedIndex="0" Width="425px"
                      FontSize="16" />
        </StackPanel>

        <StackPanel Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Top"
                    Margin="0,10,0,25" Grid.Column="2" Grid.Row="2">
            <TextBlock Text="Пароль" Margin="0,0,0,2"
                       FontWeight="SemiBold" />
            <StackPanel Orientation="Horizontal">
                <PasswordBox AttachedProperties:PasswordHelper.Attach="True" 
                             AttachedProperties:PasswordHelper.Password="{Binding Password, Mode=TwoWay}"
                             Width="350px" FontSize="16" Margin="0,0,10,0" />
                <Button Content="S" Height="28px" Width="28px" Margin="0,0,10,0" />
                <Button Content="R" Height="28px" Width="28px" />
            </StackPanel>
        </StackPanel>

        <Button Content="Войти" Command="{Binding EnterCommand}"
                Width="100px" Height="42px" HorizontalAlignment="Right" VerticalAlignment="Top" 
                Margin="0,25,0,25" FontSize="16" FontWeight="SemiBold"
                Grid.Column="1" Grid.ColumnSpan="3" Grid.Row="3" />

    </Grid>
</Window>
